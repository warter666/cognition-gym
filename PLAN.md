# 思辨健身房（cognition-gym）工程化方案

> 从参赛项目"信息加方：智能认知训练系统"转化的可落地工程。
> 前置设计文档：`agent_contest_extract/工作流v2/README.md`（闭环状态机 + 答案卡的设计依据）。

## 0. 一句话决策

**Coze 退出运行时；核心引擎自研（Python，确定性状态机编排 + 角色化 LLM 调用 + 代码化批改）；CLI 先行，日用迭代；Coze 仅在未来出现非技术用户分发需求时作为"渠道皮"接入。**

## 1. 三条路径的取舍（结论先行）

| | A. Coze 工作流 | **B. 自研引擎（推荐）** | C. Agent 原生（做成 CLI 里的 skill/子agent） |
|---|---|---|---|
| 训练循环（提示梯 L1→L4 多轮、仲裁、变式追问） | ✗ 工作流图是单向 DAG 心智，用会话变量模拟对话内循环很脆，改一次逻辑要在图上连线 | ✓ 就是普通代码，状态机可单测 | △ 可以，但状态散落在会话文件里 |
| 批改公平性 | ✗ 只能整体塞进 LLM 节点靠提示词约束，规则漂移不可测 | ✓ 位置匹配下沉到代码（difflib），LLM 只判模糊部分；幻觉风险砍半 | ✗ 批改者与用户同处一个上下文，答案卡保密、判分独立性都靠文件隔离，能做但别扭 |
| 提示词版本管理 + 回归评测 | ✗ 无法 git 化，改提示词=盲改 | ✓ 提示词是文件，评测集跑批 | ✓ |
| 模型分层降本 | △ 粒度粗 | ✓ 生成=旗舰，批改/解析/复盘=小模型 | △ 受限于宿主 agent 的模型 |
| 数据所有权（错题本/训练记录） | ✗ 在平台 | ✓ 本地 SQLite | ✓ |
| 分发给非技术用户（学生/大众） | ✓ 唯一优势 | 需加 web 皮（R2） | ✗ 仅限 agent CLI 用户 |
| 日常自用迭代速度 | 慢（改图→发布→验证） | **快（改代码→跑评测→用）** | 快 |

**决策：B 为主。** C 不作为架构，但白送一个封装：gym 本身是 CLI，你在 ZCode/Claude Code 里随时 `gym train "命题"` 就能用——agent 只是启动器，不是运行时。这正面回应了之前"claudecode 放弃"的教训：**不要让自治 agent 当运行时，要让确定性引擎当运行时**。

**为什么不是"多 agent / 子 agent"？**
训练主循环需要可复现、可评测、判分公平——自治 agent 的不确定性在这里是负债不是资产。v2 设计里的六个"角色"（解析、生成×3、批改、教练、复盘）本质是**上下文隔离 + 模型分层的提示词分工**，用普通函数 + 角色提示词文件就拿到全部收益，不需要 agent 框架的运行时。真正带工具自治的岗位只有两个：
1. **知识库策展**（RSS 抓取 → 热度卡 → 去重入库）：离线定时任务，`requests + feedparser + LLM` 就够，无需常驻 agent；
2. M2 源文献取材（R2 可选）。

## 2. 产品定义（日常使用画面）

```
$ gym train "成瘾行为的多巴胺机制"
[P0] 解析完成：主题=多巴胺与成瘾的因果关系 | 领域=神经科学/心理学
     模式=M1（近30天热度 12.4，热门路由）| 难度=40
[P2] 训练文章已生成（786 字，尾部附训练水印）。请通读后标注可疑句。
     格式：原文摘句 | 你的理由（每行一条，空行结束）
你> 因此所有成瘾行为都源于多巴胺 | 把相关说成了因果，还有其他递质通路
你>
[P3] 命中答案卡#1（位置✓ 类型✓ 解释✓）得分 4/4（100%）
     正文还埋了 1 处你没发现，集中在第 5 段附近。再试试？（提示 L1）
你> …
[P4] 全部命中。进阶复盘：该谬误在经济学中的变体是……
[P5] 变式提问：如果把场景换到社交媒体成瘾……这个推理还成立吗？
     已记录：命题/谬误类型/得分/日期 → 错题本（弱项 3 天后复现）
```

- **目标用户**：先是你自己和愿意装 CLI 的同学（MVP）；web 皮之后再谈学生/大众（R2，走决策门 G2）。
- **北极星指标**：周训练完成轮次（连续 4 周不衰减即产品成立）。
- **护栏指标**：边界合规率 = 100%（硬门，违规即下线该生成批次）；答案卡断言通过率 ≥98%；批改一致性 kappa ≥0.7。

## 3. 架构与仓库结构

```
cognition-gym/
├─ PLAN.md                  ← 本文档
├─ src/gym/
│  ├─ schema.py             # 数据契约：AnswerKey/FallacyItem/Grading/SessionState（pydantic-free，dataclass）
│  ├─ taxonomy.py           # 谬误分类表加载 + 生成物断言（type∈表、quote∈正文）
│  ├─ state.py              # 难度状态机（确定性，单测覆盖）
│  ├─ router.py             # 热度路由（确定性，时间衰减；冷启动退化为领域频率）
│  ├─ grading.py            # 代码化批改：位置匹配（difflib）+ 分数组装；LLM 只判类型/解释
│  ├─ llm.py                # LLM 接口：MockLLM（测试）+ OpenAICompatLLM（生产，stdlib 实现）
│  ├─ session.py            # 编排器：P0→P2→P3→P4→P5 一次会话循环，含答案卡校验重试
│  ├─ prompts/              # 角色提示词（从 工作流v2/prompts 清洗迁移，去 Coze 语法）
│  └─ data/taxonomy.json    # 谬误分类表（唯一事实源）
├─ apps/cli.py              # 入口：gym train "命题"
└─ tests/                   # state/router/grading/session 全链路（MockLLM）
```

**批改必须部分代码化**（本方案最重要的工程判断）：
- 位置分（用户摘句 ↔ 答案卡 quote）：`difflib.SequenceMatcher` 相似度阈值判定，确定性、零成本、可单测；
- 类型分/解释分：LLM（小模型）只对已命中位置的条目判模糊语义；
- 漏报/误伤/仲裁三类结果由代码分类后分派，LLM 无法越权改分。

**存储（MVP=单文件 SQLite，不上服务）**：
```sql
sessions(id, started_at, state_json)
answer_keys(session_id, article, key_json, mode, created_at)   -- 用户不可见
mistake_book(user_id, topic, fallacy_type, percent, date)      -- 弱项池 + 间隔重复
kb_cards(id, field, topic, mainstream_claim, keywords, date, source, distortion_potential)
```

## 4. MVP 范围裁剪（两周内日用）

| 进 MVP | 裁剪理由 |
|---|---|
| M1 瑕疵论文全链路 | 不依赖任何外部语料，冷启动最快 |
| M2 曲解论文（**源文本=用户粘贴**） | 库内取材后置；粘贴源让 M2 成本降到几乎为零 |
| P0/P3/P4/P5 + 难度状态机 + 错题本 | 闭环最小集 |
| 检索=SQLite FTS5 关键词 + 结构化表直查 | **你的 grep 观察直接变成 MVP 决策**：精确类查询（分类表、边界规则、错题本过滤）关键词检索足够；向量检索是 R2 增强，且只开一个场景——M3 桥梁概念（词面无关的跨学科关联，grep 原理上找不到） |
| **暂缓 M3** | 依赖桥梁概念对语料，质量不稳会伤信任；G3 门：概念对库覆盖 ≥30 个高频命题后再开 |

## 5. 评测（产品口径，自动化优先）

| 检查 | 方法 | 门限 |
|---|---|---|
| 答案卡可批改性 | 代码断言：每个 quote 能在正文逐字找到；type ∈ 分类表 | 100%（重试 1 次后仍失败则丢弃重生成） |
| 边界合规（四条硬线） | 规则检查（数字/引文出现即预警）+ LLM judge 抽检 | 100% / 抽检 ≥95% |
| 批改一致性 | 同批 30 例人工标注 vs 引擎判分，kappa | ≥0.7 才上线新提示词 |
| 训练效果 | 标准题集（10 题，难度受控）前后测 | 弱项类型复现得分提升 |

评测集与提示词同仓 git 化；`gym eval` 一条命令跑批（R1 内提供）。

## 6. 路线图（单人兼职节奏，按周交付）

| 周 | 交付 | 完成定义（DoD） | 状态 |
|---|---|---|---|
| W1 | 骨架 + 契约 + 确定性模块 | state/router/grading 单测绿；schema 定稿 | ✅ 19 用例绿 |
| W2 | M1 全链路 CLI | 自用 10 轮不出崩溃；答案卡断言生效 | ✅ 断言+重试已实现，待接真实模型自用 |
| W3 | 批改代码化收口 + 评测集 v0 | kappa 首测 ≥0.7；`gym eval` 跑通 | ✅ 骨架（kappa 函数+样例集+live 模式）；真实模型 kappa 待测 |
| W4 | M2（粘贴源）+ 错题本 + 间隔重复 | 弱项复现逻辑生效；两周记录可导出 | ✅ M2+SQLite 弱项池/到期复现完成（提前） |
| W5 | 策展管线（RSS→热度卡→入库） | 热度路由吃真库；一周无人值守稳定 | ◐ 解析/抽取/去重入库完成；在线抓取与无人值守待接 |
| R2 | 向量检索（仅桥梁概念）、M3、web 皮、Coze 渠道皮（G2） | 各自决策门 | 未开始 |

**单轮成本估算**：生成（旗舰）+ 批改（小模型）+ 复盘（小模型）≈ 每轮几分钱量级；同命题 24h 缓存命中时更低。

## 7. 风险与对策

| 风险 | 对策 |
|---|---|
| 生成质量方差（quote 对不上正文、谬误超表） | 答案卡断言 + 1 次自动重试，失败丢弃重生成（已实现于 session.py） |
| 批改仲裁争议（用户标注既非谬误也非干扰项） | arbitrated 通道不扣分、复盘节点裁决，保护表达欲 |
| 内容安全 | 四条硬线进生成提示词 + 水印强制 + LLM judge 抽检；医疗/金融命题 P0 直接拒 |
| 知识库冷启动（热度路由无数据） | 领域频率兜底 + 手动种子库 50 条 |
| 提示词回归 | 评测集跑批门槛（kappa ≥0.7），失败禁止替换上线 |

## 8. 提示词资产迁移清单（工作流v2 → 本仓库）

| 来源 | 去处 | 改造 |
|---|---|---|
| prompts/01-命题解析 | src/gym/prompts/parser.md | 去 Coze 变量语法，改 .format 占位 |
| prompts/02-M1 | src/gym/prompts/m1_generator.md | 同上；输出 JSON 契约与 schema.py 对齐 |
| prompts/02b-M2 / 02c-M3 | R2 迁移 | — |
| prompts/03-批改 | 拆两半：位置匹配→grading.py（代码）；类型/解释→prompts/grader.md | **批改提示词只保留模糊判定职责** |
| prompts/04 / 05 | prompts/coach.md / reviewer.md | 去 Coze 接线段 |
| prompts/00-代码节点 | state.py / router.py（已实现） | Python 3.13 落地 |
| prompts/07-热度卡 | R1-W5 策展管线复用 | — |
